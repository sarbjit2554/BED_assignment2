
export interface Branch {
    id: string; // Unique identifier for the branch (could be a string or number)
    name: string; // Name of the branch
    address: string; // Address of the branch
    phone: string; // Contact phone number of the branch
  }
  